---
import { Picture } from "astro:assets";
import { Icon } from "astro-icon/components";
import heroImage from "@/assets/hero.webp";
import Link from "@/components/shared/link.astro";
---

<main
  class="grid lg:grid-cols-2 place-items-center pt-16 pb-8 md:pt-12 md:pb-24"
>
  <div class="py-6 md:order-1 hidden md:block">
    <Picture
      src={heroImage}
      alt="Picture Goal of years"
      widths={[200, 400, 600]}
      sizes="(max-width: 800px) 100vw, 620px"
      loading="lazy"
      formats={["avif", "webp"]}
    />
  </div>
  <div>
    <h1
      class="text-5xl lg:text-6xl xl:text-7xl font-bold lg:tracking-tight xl:tracking-tighter"
    >
      Una forma para cumplir tus metas
    </h1>
    <p class="text-lg mt-4 text-slate-600 max-w-xl">
      <strong>Ziele</strong> es una herramienta para gestionar tus metas, basandose
      en una nueva estrategia de gestión. Con esta aplicación, podrás crear y gestionar
      tus metas de forma organizada y eficiente.
    </p>
    <div class="mt-6 flex flex-col sm:flex-row gap-3">
      <Link
        href="#register"
        class="flex gap-1 items-center justify-center"
        rel="noopener"
        style="customPrimary"
      >
        <Icon class="text-white size-8" name="bx:bx-timer" />

        Inicia desde hoy
      </Link>
    </div>
  </div>
</main>
